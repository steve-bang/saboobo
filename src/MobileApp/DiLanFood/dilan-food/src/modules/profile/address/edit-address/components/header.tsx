import React, { FC } from "react";
import { Header } from "zmp-ui";

export const HeaderEditAddress: FC = () => {
    return (
        <Header title="Sửa Địa chỉ" className="text-white bg-primary" showBackIcon={true} />
    )
}
